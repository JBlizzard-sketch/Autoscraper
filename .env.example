# =====================================================
# 🔒 SECURITY WARNING
# =====================================================
# This is an EXAMPLE file. DO NOT commit your actual .env file to git!
# 
# To use:
# 1. Copy this file to .env (already in .gitignore)
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. NEVER commit the .env file to version control
#
# The .env file is already in .gitignore for your protection
# =====================================================

# =====================================================
# Supabase Configuration
# =====================================================
# Get these from your Supabase project settings (https://supabase.com/dashboard)
# Project Settings > API

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Anon/Public key - safe to use in client-side code
SUPABASE_ANON_KEY=your-anon-key-here

# Service Role key - for server-side operations and data import
# ⚠️  NEVER expose this publicly - only use server-side
SUPABASE_SERVICE_KEY=your-service-role-key-here

# =====================================================
# Database Connection
# =====================================================
# PostgreSQL connection string from Supabase
# Project Settings > Database > Connection String > URI
# Use "Transaction" or "Session" mode for serverless compatibility
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres

# =====================================================
# Application Settings
# =====================================================
NODE_ENV=development

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================
# 1. Create a Supabase project at https://supabase.com
# 2. Run the SQL schema: Copy sql/SUPABASE_PRODUCTION_SCHEMA.sql into Supabase SQL Editor
# 3. Import data ONCE: tsx scripts/import-csvs.ts (with SUPABASE_SERVICE_KEY set)
# 4. Add these environment variables to your Replit project
# 5. Restart the application - database is ready!
#
# For future agents: Just enter these credentials, no import needed!
